<wpfui:FluentWindow x:Class="EasyShare.App.Views.ReceiveDialog"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="Eingehende Übertragung" Width="420" Height="380"
  WindowBackdropType="Mica" ExtendsContentIntoTitleBar="True"
  ResizeMode="NoResize" WindowStartupLocation="CenterOwner">

  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <wpfui:TitleBar Grid.Row="0" Title="Eingehende Übertragung"/>

    <TextBlock Grid.Row="1" x:Name="SenderText" FontSize="16" FontWeight="SemiBold" Margin="0,12,0,4"/>

    <Border Grid.Row="2" Background="#1a1a1a" CornerRadius="6" Padding="12" Margin="0,8,0,0"
            BorderBrush="#333" BorderThickness="1" MaxHeight="180">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl x:Name="FileList"/>
      </ScrollViewer>
    </Border>

    <TextBlock Grid.Row="3" x:Name="TotalSizeText" FontSize="12" Foreground="Gray" Margin="0,8,0,0"/>

    <CheckBox Grid.Row="4" x:Name="TrustCheckBox" Content="Immer von diesem Gerät akzeptieren"
              Margin="0,12,0,0" VerticalAlignment="Center"/>

    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
      <Button x:Name="RejectButton" Content="Ablehnen" Padding="16,6" Margin="0,0,8,0" Click="Reject_Click"/>
      <Button x:Name="AcceptButton" Content="Annehmen" Padding="16,6" Click="Accept_Click"/>
    </StackPanel>
  </Grid>
</wpfui:FluentWindow>
